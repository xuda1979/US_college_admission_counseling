version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: us-college-counseling:web
    ports:
      - "8000:8000"
    env_file:
      - backend/.env.docker
    volumes:
      - ./backend/data:/app/data
    restart: unless-stopped
